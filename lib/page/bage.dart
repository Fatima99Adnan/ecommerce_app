import 'package:flutter/material.dart';

class Bage extends StatelessWidget {
  const Bage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Bage Screen'));
  }
}
