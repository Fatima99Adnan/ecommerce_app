import 'package:flutter/material.dart';

class Shope extends StatelessWidget {
  const Shope({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Shope Screen'));
  }
}
